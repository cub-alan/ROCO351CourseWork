

import runDynaQEpisode.*

episode = 0;

while episode<100
    episode = episode+1;
    [qtable,model_reward,model_nextstate,visited,steps] = runDynaQEpisode(qtable,model_reward,model_nextstate,visited);

end